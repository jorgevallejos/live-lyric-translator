node_modules/
dist/
dist-electron/
release/
.DS_Store
public/*.json
!public/config.json

# Claude Code — local-only config (commands/ is intentionally tracked)
.claude/settings.local.json
.claude/sessions/
.claude/worktrees/

# ASR-following spike (2026-07-03, throwaway) — local scratch: 1.1 GB venv + data.
# Canonical copy of the report + reusable scripts lives on the spike/asr-following branch.
spike/